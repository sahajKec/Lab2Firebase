specVersion: "v1beta"
serviceId: "lab2firebase"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "lab2firebase-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
